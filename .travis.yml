dist: bionic
language: node_js

node_js:
  - "12"

script:
  - set -e
  - npm test
  - make docker-test

jobs:
  include:
    - stage: test
      name: Node test
    - stage: test
      name: Browser test
      env: TEST_BROWSER=true
      addons:
        chrome: stable
